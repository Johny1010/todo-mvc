{
  "name": "todo-mvc",
  "version": "1.0.0",
  "description": "todo for practice",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "grunt": "~0.4.5"
  }
}
